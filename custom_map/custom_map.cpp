#include <iostream>
#include <fstream>

#include"AVL_hashTable.h"

int main()
{
	//the candidate contains in the exact same order: surname, name, CNP, grade
	//the candidates can be read from the file with the function: readFromFile(input);

    std::ifstream input("input.txt");

	AVL_hashTable map;
}
